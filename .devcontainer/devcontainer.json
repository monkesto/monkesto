{
    "name": "Dev",
    "dockerComposeFile": "docker-compose.yml",
    "service": "dev",
    "workspaceFolder": "/workspaces/${localWorkspaceFolderBasename}",
    "remoteUser": "monkesto",
    "features": {
        "ghcr.io/devcontainers/features/node:2": {}
    },
    "postCreateCommand": "sudo chown -R $(id -u):$(id -g) node_modules target && npm ci && ./scripts/install-hooks.sh"
}
